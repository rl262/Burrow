# Burrow dashboard (FastAPI) -- pinned dependencies
# Pinned dependency set; adds itsdangerous (signed session cookie)
# and python-multipart (HTML form parsing).
fastapi==0.115.4
uvicorn[standard]==0.32.0
jinja2==3.1.4
httpx==0.27.2
itsdangerous==2.2.0
python-multipart==0.0.12
